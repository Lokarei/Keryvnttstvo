% --- КОДИРОВКИ И ЯЗЫК ---
\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[ukrainian]{babel}
\usepackage{cmap} 

% --- ГРАФИКА И ВЁРСТКА ---
\usepackage[left=2.5cm, right=1.5cm, top=2cm, bottom=2.5cm]{geometry}
\usepackage{graphicx}
\usepackage{float}    
\usepackage{caption}  
\usepackage{etoolbox} 

% --- НАВИГАЦИЯ (ПЕРЕНЕСЕНО ВВЕРХ) ---
\usepackage[colorlinks=true, linkcolor=blue, linktoc=all]{hyperref}
\usepackage{fancyhdr}

% --- ОПРЕДЕЛЕНИЕ ГРАФИЧЕСКИХ БЛОКОВ ---
\usepackage[most]{tcolorbox} 

\newtcolorbox{mybox}[1]{
  colback=blue!5!white,
  colframe=blue!75!black,
  fonttitle=\bfseries,
  title=#1,
  arc=2mm,
  enhanced,
  drop shadow
}

% --- НАСТРОЙКИ ТЕКСТА ---
\shorthandoff{"} 

% --- КОЛОНТИТУЛЫ (ТЕПЕРЬ ОНИ ВИДЯТ HYPERLINK) ---
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.5pt}
\fancyhead[L]{\footnotesize Керівництво зі збирання "ОРІОН"}
\fancyfoot[C]{\hyperlink{toc_link}{\thepage}} 

% Эти команды исправляют баг, когда на страницах с главой исчезают колонтитулы
\patchcmd{\chapter}{\thispagestyle{plain}}{\thispagestyle{fancy}}{}{}
\patchcmd{\tableofcontents}{\thispagestyle{plain}}{\thispagestyle{fancy}}{}{}
% --- НАЛАШТУВАННЯ ПІДПИСІВ ---

% 1. Змінюємо скорочене "Табл." на повне "Таблиця" для української мови
\addto\captionsukrainian{\renewcommand{\tablename}{Таблиця}}

% 2. Налаштовуємо розділювач (тире замість двокрапки)
\DeclareCaptionLabelSeparator{dash}{ --- } % Можна змінити на " — " або " - "
\captionsetup[table]{
    labelsep=dash,       % Використовувати наше тире як розділювач
    justification=centering, % Встановлює підпис по центру
    singlelinecheck=false    % Щоб підпис не центрувався, якщо він короткий
}
